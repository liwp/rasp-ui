RASP To Do List
===

In no particular order…

- add layers
- go-to UI with BGA turn point DB
- remove header and place time / loading indicator somewhere else to save
  space (some animated fade-in/out label?)

## Layout

It seems like the current Mobile Safari `100vh` behaviour (where the chrome
hides some of the page content) is intentional, so I won't be trying to fix
this at the moment. The workaround is to use Chrome.

- work out if we can fix sizing on mobile
  (https://www.creativebloq.com/html5/12-html5-tricks-mobile-81412803)
